GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash
# Max concurrent Gemini batch calls (default 4). 20 photos = 3 batches, so
# values above 3 have no effect at the current photo limit.
MAX_CONCURRENCY=4
