# Phase 1 dependencies
opencv-python-headless==4.10.0.84
google-generativeai==0.8.3
python-dotenv==1.0.1

# eye_openness is stubbed (returns 5.0) until a Pi ARM64-compatible
# alternative to MediaPipe is identified. See core/score_tech.py.
